torch>=2.1
# Optional: only needed for HFBackend (real backbone LLM). The core library,
# MockBackend, and unit tests run on torch alone.
transformers>=4.40
# Optional: TensorBoard logging for training runs (examples/*, --logdir).
tensorboard>=2.10
